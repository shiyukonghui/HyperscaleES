//! 反对称配对的噪声生成支持（本项目 patch）。
//!
//! 训练热路径的 LoRA 噪声按**样本对**反对称配对（样本 `n/2+i` 的噪声 = 样本 `i`
//! 的取负），因此只需生成**前半**张量（`(n/2, r, b)`），配对由消费方
//! （`forward_batched_lora_half` / `lora_einsum_pair_half`）隐含施加——生成量减半
//! （fc1 B' 2.4GB → 1.2GB，gen 阶段 ~17ms → ~7ms/chunk）。
//!
//! 历史：曾实现「一次内核生成完整配对张量」的反对称内核（后半线程重新生成同一
//! 序列并取负）与「生成一次、双流写两半」的方案，实测均明显慢于 plain 内核生成
//! 前半 + 消费方配对（训练循环 A/B：0.19s vs 0.21s vs 0.28s/epoch），已废弃。
