//! HyperScaleES envs — environment/data + reward helpers for evolutionary training.
//!
//! Port of the Python `environments/.py` modules to Rust (burn 0.21, flex backend).

pub mod snn_mnist;
pub mod llm_bandits;
